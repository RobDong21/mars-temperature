import styled from 'styled-components'

import { FONTSIZE } from './fontsize.constant'

const Body = styled.p`
  font-size: ${FONTSIZE.S};
  text-align: center;
  margin-bottom: 80px;
`

export { Body }